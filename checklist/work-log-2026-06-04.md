# 작업 기록 — 2026-06-04

## 세션 개요
- **시작 시점**: HEAD `8aef2e9` (2026-06-02 인사이트 개편 세션 직후). 운영 도메인 `today-tactics.co.kr`, 데이터 5/31(R14)까지, 작업공간 탭(전술판/경기예측/팀/선수) 가동.
- **종료 시점**: ① 운영 도메인 `www.today-football-tactics.xyz` 전환 ② 단일 팀 종합 분석 대시보드 신설+팀 탭 통합 ③ RESUME/work-log 기록.
- **commits (이번 세션)**: `d4f7399`(도메인) → `b434f35`(팀분석 대시보드) → `6c07945`(팀 탭 통합) → 본 문서.
- **운영 반영**: 변경된 static/templates만 scp + `systemctl restart today_tactics` + 공개 도메인 라이브 검증. 정적 자산 `?v=` 증가(style.css v72 / analytics.js v2 / workspace.js v5).
- **데이터**: 5/31 이후 6/4까지 경기 없음(리그 휴식기) → 5/31까지가 정상 최신. 다음 라운드 6/5~6/7 → 6/8(월) 새벽 자동 수집 반영 예정.

---

## 1. 운영 도메인 전환 — today-tactics.co.kr → www.today-football-tactics.xyz
- **결정**: canonical = `www.today-football-tactics.xyz`(사용자 지정). apex·구 도메인·IP 직접 접근은 전부 301 통합. 구 도메인은 301로 보존(실제론 구 도메인 공개 DNS 이미 소멸 상태였음).
- **선행 블로커**: 신 도메인 DNS 미전파(레지스트리 NS 위임 지연). 가비아 권한 NS는 정상 응답하나 공개 리졸버 NXDOMAIN → 약 20분 대기 후 전파 확인하고 진행(전파 전 certbot 시도 금지 — Let's Encrypt 실패 rate limit 회피).
- **인증서**: 서버 `/var/www/certbot` 생성 → acme-challenge 200 사전검증 → `certbot certonly --webroot -w /var/www/certbot -d today-football-tactics.xyz -d www.today-football-tactics.xyz`. apex 첫 `-d` → live 디렉터리 `today-football-tactics.xyz`(nginx 경로와 일치). SAN apex+www, 자동 갱신 등록, 만료 2026-09-02.
- **nginx**: `deploy/today_tactics.nginx` 재작성 — ①HTTP 80 모든 도메인 → 301 ②구 도메인 HTTPS(구 cert) → 301 ③apex HTTPS → 301 ④canonical www 본서버(앱 프록시). 서버 적용 전 기존 conf `/tmp` 백업 → `nginx -t` 통과 → reload.
- **검증**: www 200, apex 301→www, http 301→https, 외부 cert(Let's Encrypt 신뢰) 확인.
- **코드**: `main.py:103` 쿠키 secure 주석만 갱신(동작 무관). OAuth redirect는 `url_for(_external=True)` Host 동적 → 코드 수정 불필요.
- **⚠️ 미완**: Google/Kakao/Naver 콘솔에 `https://www.today-football-tactics.xyz/auth/callback/{provider}` redirect URI 등록 전까지 **로그인만 깨짐**. 사이트 본체/데이터는 정상. (사용자 직접 등록 필요 — 콘솔 접근권.)

---

## 2. 단일 팀 종합 분석 대시보드 — 신설 + '🛡 팀' 탭 통합
- **문제 진단**: 사용자가 "비교만 많고 팀별 분석이 없다" 지적. 추적 결과:
  - `static/js/analytics.js`(옛 팀분석=상대팀별 1차트)가 **index.html에 미로드**(스크립트 태그 없음) = 죽은 코드.
  - 진입 버튼 `#btn-analytics`는 `#analytics-widget-area { display:none }`(style.css)에 갇힌 **고아 버튼** → 클릭 불가.
  - 리디자인 때 "비교와 중복"이라며 제거됐던 것(`workspace.js` 주석 근거). 그러나 백엔드는 멀쩡:
    - `/api/team-analytics`: 상대팀별·월별 승률·홈/원정·**날씨별 승률**(4종, 프론트가 1종만 쓰고 3종 버림).
    - `/api/league-rankings`: 고급 11지표 + 리그 순위.
    - `/api/team-trend`: 경기별 득/실/결과/누적승점 시계열.
- **방향(사용자 선택)**: "결과 + 스킬 통합" 풀버전.
- **구현(프론트 중심, 백엔드 0줄)**:
  - `analytics.js` 전면 재작성 → `#team-analysis-modal`(다크 테마) 2탭.
    - **📋 결과 분석**: 시즌 득/실 트렌드(누적승점 막대) · 월별 승률 · 홈/원정 성적 · 상대팀별 전적(스택바+표) · 날씨별 승률(기온/습도/풍속 바).
    - **📡 스킬 프로필**: 고급 11지표 **레이더**(순위→리그 백분위, 평균 50% 점선 기준선) + 지표별 값·**리그 순위 뱃지**·평균 대비 막대(흰 눈금=리그 평균).
  - 아이덴티티+시즌 요약(엠블럼·폼핀·경기/승점/전적/득실/PPG), 연도 필터, 탭/연도 전환 시 차트 resize.
  - CSS `style.css` +다크 테마 블록(`.ta-*`), 재사용 `.tc-panel` 등은 모달 스코프 다크 오버라이드.
- **🐛 진입점 1차 실수 → 2차 수정(`6c07945`)**: 1차(`b434f35`)는 모달을 숨겨진 고아 버튼에 연결해 사용자가 못 찾음. 팀 비교와 동일 패턴으로 재통합:
  - `workspace.js`: `move("team", ["team-analysis-modal","team-compare-modal"])` 둘 다 '팀' 탭 인라인.
  - '🛡 팀' 탭에 **모드 토글** `[📊 단일 팀 분석] / [⚖️ 팀 비교]`, 기본=분석. `setTeamMode()`가 모달 show/hide + 최초 1회 기존 트리거 click 으로 초기화.
  - `analytics.js`: 인라인(`.ws-panel` 자식) 시 닫기/Esc/백드롭으로 숨지 않도록 가드.
- **검증**: 헤드리스(Playwright)로 실제 '팀' 탭 클릭 → 기본 분석 노출 → 팀 선택 렌더 → 토글 비교/복귀까지 **콘솔 에러 0 / 4xx 0**. K1(ulsan) 결과·스킬 두 탭 스크린샷 확인. 운영 라이브 검증 완료.
- **버그 2건(검증 중 발견·수정)**: ① 전역 `.hidden` 미존재 → 탭 패널 안 숨던 문제(스코프 규칙 추가) ② 엠블럼 src 접두사 누락 404(`/static/img/emblems/` 추가).

---

## 3. 메모/주의
- 이번 배포는 서버 git pull 아닌 **scp 직접 덮어쓰기** → 서버 작업트리가 origin과 어긋날 수 있음. 다음 세션에서 서버 `git fetch && git status` 정합성 확인 권장(어긋나면 `git reset --hard origin/main` 후 재시작).
- 운영 IP/SSH 키/이메일은 본 문서에 미기재(보안). Claude 메모리 `deployment_target`·`project_domain_migration` 참조.
