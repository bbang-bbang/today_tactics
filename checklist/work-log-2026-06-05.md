# 작업 기록 — 2026-06-05

## 세션 개요
- **시작 시점**: HEAD `fd11be0` (2026-06-04 도메인 전환 + 팀 분석 대시보드 세션 직후). 운영 `www.today-football-tactics.xyz`, K1 데이터 R15(2026-05-17)까지.
- **종료 시점**: ① 팀 분석 대시보드 가독성 개선 ② **team-analytics K1 tournament_id 버그 수정**(핵심) ③ 심화 인사이트 탭 신설(5종→프루닝 4종) ④ 팀 탭 아이콘 수정 ⑤ RESUME/work-log 기록.
- **commits (이번 세션)**: `b202619`(가독성 개선) → `ee963fb`(아이콘) → `1819fb0`(심화 인사이트 + 버그수정) → `fe63b9d`(파울 프루닝) → 본 문서.
- **운영 반영**: 매 커밋마다 **서버 `git fetch && git reset --hard origin/main` + `systemctl restart`** + 라이브 검증. (2026-06-04 work-log의 "서버 git 정합성" 우려 → 이번엔 git pull 방식으로 정합성 유지. 운영 작업트리 clean 확인됨.) 정적 자산 `?v=` 증가: style.css v72→75, analytics.js v2→5.
- **데이터 현황**: K1 R1~R15(90경기) 완비. **R16은 2026-07-04 재개**(2026 월드컵 6/11~7/19 휴식기). 05-17~오늘 예정 경기 0건 → 미수집 아님, 정상.

---

## 1. 팀 분석 대시보드 가독성 개선 (`b202619`)
사용자 지적: ① 상대팀별 전적에 차트가 2개(가로누적막대+표) 중복 ② 단독 팀 분석 수치 가독성 부족 — "전체 중 몇 위인지".
- **상대팀별 전적**: 가로누적막대(`ta-vs` 캔버스) 제거 → **표 단일화**(승률 내림차순, 만만한 상대 위).
- **요약 카드 리그 순위 칩**: 득점·실점 카드에 `리그 N/12위` 칩 추가(상위 초록/중위 노랑/하위 빨강). league-rankings의 `gf_per_game`/`ga_per_game` 정확 순위만 사용. 승점 카드는 시즌 PPG 순위가 백엔드에 없어(최근5경기 폼뿐) 미적용(P4 오해 방지).
- **신규: ⚖ 득실 마진 분포**: trend.matches 점수차 7버킷(대승 초록→무 회색→대패 빨강) + '살얼음형/압도형/취약' 자동 라벨. 프론트 집계, 백엔드 0줄.

## 2. 팀 탭 🛡 아이콘 컬러 수정 (`ee963fb`)
- `🛡`(U+1F6E1)은 text-default 이모지 → 변형 선택자 없으면 모노크롬/tofu. 옆 탭(🎯📊👤)·팀 비교(⚖️)와 동일하게 **VS16(U+FE0F)** 추가 → `🛡️`. 라이브 바이트 검증.

## 3. 🐛 핵심 버그 — team-analytics가 K1 팀에 엉뚱한 대회 표시 (`1819fb0`)
사용자가 "K1 2026 데이터가 완벽하게 안 나온다"고 느낀 **실제 원인**.
- **진단**: `/api/team-analytics`가 모든 쿼리에서 `tournament_id=777` **하드코딩**. 형제 함수 `team-trend`는 `tid = 410 if K1 else 777`로 올바름. → K1 팀(ulsan 등)은 리그(410)가 아닌 777 대회를 조회, 2026이 거의 빈 채 표시.
  - 검증: ulsan(ss_id=7653) tid=410 = 158 리그경기인데, 777 = 다른 대회(2020~2025, 2026 0경기). 777엔 ulsan vs K1 팀 경기도 섞여 깔끔한 리그 구분 아님.
- **수정**: `tid = 410 if league=="K1" else 777` 분기 + 6개 서브쿼리(available_years/vs_opponents×2/by_month×2/by_year_ha×2/weather) 파라미터 바인딩화.
- **추가 버그**: team-analytics는 `home_score IS NOT NULL` 필터 누락 → 미진행 미래 경기(7~10월)가 승률 분모에 섞임(ha 2026 away가 16경기로 부풀고 W/D/L은 7건만). team-trend엔 있던 필터를 6개 서브쿼리에 추가. → ulsan 2026 home 8/away 7(총 15, 전부 분류), 월별 2~5월만 정상화.
- **회귀 확인**: K2(suwon) 정상 동작(tid=777 경로 유지).

## 4. 신규 — /api/team-insights + '📈 심화 인사이트' 탭 (`1819fb0`, 프루닝 `fe63b9d`)
기존 지표(트렌드/마진/월별/홈원정/상대전적/날씨/스킬레이더)와 **비중복** 신규 인사이트. 새 엔드포인트 1개로 오케스트레이션, 프론트 새 탭.
- **백엔드 `get_team_insights`**(goal_events + match_player_stats, 치른 경기만): goal_timing(15분6버킷 득/실), first_goal(선제득점 vs 선제실점→W/D/L), xg_cumulative(누적 xG vs 실득점), scorers(선수별 득점+도움 top8), ~~discipline~~(프루닝됨).
- **프론트 4종 차트**(파울 제거 후): ⏱ 득점·실점 시간대 · ⚡ 선제골 영향 · 🎯 xG vs 실제 득점 누적 · 🥇 득점 기여 분포.
  - 예시(ulsan 2026): 선제득점 7승0무0패 / 선제실점 2승1무4패, 22골 vs xG 26.4(−4.4), 야고6·이동경5+3.
- **fail-soft**: insights fetch는 `.catch(()=>({}))` — 실패해도 대시보드 전체 안 죽음.
- **데이터 정합성 발견·수정**: mps에 **팀 비참가 경기 오태깅 행**(ulsan team_id=7653인데 비참가 경기 382건). `team_id=?`만으론 흡수 → xG/scorers/discipline 쿼리에 `(e.home_team_id=? OR e.away_team_id=?)` 참가 필터 추가(540→158경기 정상화). **2026 단일시즌은 우연히 오염 없어 연도필터 시엔 안 드러남 — 전체 집계에서만 터짐**. 같은 패턴 league-rankings xG도 잠재 영향(미점검).
- **mps 데이터 한계**: yellow_cards/red_cards 전부 NULL(미수집), player_name NULL(→players.name_ko 조인), fouls는 최근 시즌만 채워짐.
- **프루닝(`fe63b9d`)**: 사용자 판단으로 🥊 파울 성향 제거(카드 미수집+과거 파울 희소로 가치 낮음). 득점 기여를 전체너비로 재배치. 4종 유지.

## 5. 메모/주의
- **Windows dev 서버 위생**: Bash `pkill -f "python main.py"`가 안 먹혀 :5000에 서버 3개 동시 LISTEN → 옛 코드 서빙으로 "수정 반영 안 됨" 가짜 버그(응답이 home은 필터/away는 미필터로 내부 모순). 검증은 **PowerShell `Stop-Process` + 새 포트(5001) 단일 인스턴스**가 안전. 인메모리 캐시(`_API_CACHE`)와는 별개 원인.
- 운영 IP/SSH 키/이메일은 본 문서 미기재(보안). Claude 메모리 `deployment_target`·`reference_mps_team_id_contamination`·`feedback_windows_dev_server_hygiene` 참조.
- 검증: 매 변경 헤드리스(Playwright) 실제 팀 탭 동선 렌더 + 콘솔 에러 0, 운영 라이브 검증 완료.
