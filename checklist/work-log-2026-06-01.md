# 작업 기록 — 2026-06-01

## 세션 개요
- **시작 시점**: HEAD `16f00dc` (6/1 새벽 보안 강화 세션 직후). K1+K2 데이터 5/25(R13)까지, 전술판+예측+선수/팀 분석 모달 가동.
- **종료 시점**: 화면 정보구조(IA) 전면 개편(작업공간 탭) + 팀 차트 정비·다크 통일 + H2H 득점시간 + 라운드예측 성능 + 운영 배포 완료.
- **commits (이번 세션 3건)**: `753bf24 → 1a00ed9 → 020bdbf`
  - `753bf24` deploy.sh root→rocky + 2026-06-01 데이터 동기화
  - `1a00ed9` refactor: 미사용 import·빈 f-string·데드코드 정리 (ruff)
  - `020bdbf` feat: 작업공간 탭 IA 개편 + 팀 차트 정비/다크화 + 성능·UX 개선
- **운영 반영**: 전부 CI 자동배포 + 서비스 재시작으로 https://today-tactics.co.kr 라이브 검증 완료.

---

## 1. 데이터 수집 (R14) + 서버 자동수집 확인

### 1-1. R14 증분 수집
- 로컬 `update_data.py` 18-STEP 전체 정상(약 22분). 신규 16경기(390→406), R14 8경기 스코어/라인업/포메이션 백필.
- 5/30 5경기는 히트맵·avg_positions·슛맵 완비, **5/31 3경기(전남·화성·안산)는 SofaScore 포지셔널 미발행**으로 비어있음 → 다음 주 cron 자동 보강([[feedback_update_data_partial_failures]] 패턴).

### 1-2. "운영 반영" 요청 → 서버 자동수집 발견 (중요)
- 운영 DB가 이미 로컬과 **완전 동일**(events/heatmap/mps/avg_pos 카운트 일치, 둘 다 latest=5/31).
- 원인: 서버 rocky crontab `0 5 * * 1` = **매주 월 05:00 update_data.py 자체 실행**. 6/1(월) 새벽에 이미 수집 완료(players.db mtime 05:20).
- → **데이터 수동 push 불필요**. 메모리 신설 [[project_server_auto_update]].
- 부수: 접속이 root가 아니라 **rocky** 계정임을 확인(root 시도 시 fail2ban 차단). 한동안 SSH 막혀 가비아 VNC/재기동으로 복구. SSH(22) DROP 중에도 80/443은 열려 502(앱 다운)을 포트 스캔으로 진단 → 재기동으로 복구.

---

## 2. 배포 인프라 — deploy.sh root→rocky (`753bf24`)
- 과거 자동화가 전부 `root@`라 root SSH 차단 후 동작 불가했음 → `rocky@` + 키 명시(-i) + `sudo systemctl restart` + 헬스체크로 복구.
- 서비스가 `User=rocky`라 `/opt/today_tactics` 직접 업로드 가능, restart만 sudo.

---

## 3. 코드 정리 — ruff (`1a00ed9`)
- ruff 안전 자동수정 71건 + 수동 1건: 미사용 import(F401) 29, 빈 f-string(F541) 41, 중복재정의(F811) 1, 데드코드(F821) 1(`scan_suspicious_formations.py`의 `X if False else Y`).
- 검증: 66개 .py 전부 py_compile 통과, main.py import OK.
- 미적용(보류): E722 bare-except(외부 API 격리 의도), E701/E702 compact 스타일, F841 — 회귀 위험.

---

## 4. 화면 정보구조(IA) 전면 개편 — 작업공간 탭 (`020bdbf` 핵심)

### 4-1. 롱스크롤+모달 8개 → 상위 작업공간 탭
- 신규 `static/js/workspace.js`: **전술판 / 경기예측 / 팀 / 선수** 4탭 컨트롤러.
  - 설계 원칙 "DOM은 재배치(appendChild)하되 내부 id 보존" → 각 모듈 렌더 로직 거의 무수정.
  - onShow 훅: 전술판 탭 표시 시 `window.__ttResize()`(app.js에서 노출), 차트 탭은 `Chart.getChart(canvas).resize()` 일괄. `#canvas-container`에 ResizeObserver 안전망.
  - 매치 선택 스트립은 탭바 위에 상주(전 탭 공통 맥락).
- 매치 선택 후 **스트립 접기**(localStorage `tt_match_strip_collapsed`) + 매치 카드 클릭 시 자동 접기 → 전술판 세로 공간 회복.
- 선수 개별분석 → **전술판 탭**(캔버스 우측), 인사이트 → 선수 탭, 팀분석·비교 → 팀 탭 인라인(모달 제거, CSS로 정적화).

### 4-2. 핵심 제약 & 완화 (확인됨)
- 캔버스 크기가 컨테이너 가시성 의존(ResizeObserver 없었음) → 탭 표시 시 resize 호출로 해결. 탭 왕복 후 캔버스 width 유지 검증.
- Chart.js는 display:none→표시 시 자동 리사이즈 안 함 → onShow에서 일괄 resize.
- analytics/team_compare는 모달 id 없으면 early-return → 인라인 시 동일 id 보존으로 무수정.

---

## 5. 팀 탭 차트 정비 + 다크 테마 통일

### 5-1. 전문가 패널 감사 → 중복 차트 제거
- 제거: 날씨 3종(표본 부족·저신뢰), 홈/어웨이(팀비교와 중복), 월별 승률(트렌드와 중복), 공수밸런스 산점도(스탯바·레이더와 중복), **팀 분석(상대팀별 승률) 섹션 전체**.
- `analytics.js`는 팀분석 제거로 **미사용 파일화**(스크립트 태그 제거). ※ 추후 파일 삭제 + CLAUDE.md 문서 정리 필요(백로그).

### 5-2. 신규 인사이트
- **골 타이밍 두 팀 비교**(득점/실점 시간대 오버레이) — `/api/goal-timing` 기존 엔드포인트로 백엔드 무변경.
- 팀 선택 통합: 팀 비교의 팀 A 선택이 (제거 전) 팀분석도 구동하도록 했다가, 팀분석 제거로 정리.

### 5-3. 카테고리 컬러 코딩 + 다크 전환
- 패널별 accent 컬러(보라=종합/초록=폼/앰버=트렌드/주황=선제득점/파랑=스탯/분홍=골타이밍/골드=순위/빨강=H2H) 좌측 보더+헤더.
- **team_compare 라이트 모달 → 다크 테마 전환**: `.tc-modal-content` 테마 변수(--tc-card #fff→#16213e, --tc-ink 어두움→밝음 등) + 하드코딩 라이트 배경(셀렉트·매치업 헤더·H2H 카드·득점자 칩·연도필터·테이블) 다크 오버라이드. **차트 텍스트색(#1f2d47 등)도 team_compare.js에서 밝은색으로** (안 그러면 다크 배경에서 안 보임).

---

## 6. H2H 맞대결 — 득점 시간 표시
- `goal_events` 테이블(선수별·경기별 분, 4933건/1760경기/894선수, K1+K2 2020~2026, minute 결측 0) 활용.
- `/api/h2h-matches` 득점자를 mps → **goal_events(분 포함)** 로 전환. **goal_events 없는 경기는 mps 폴백**(커버리지 손실 0, goal_events는 스코어 경기의 ~68% 커버). 자책골은 수혜팀 귀속.
- 프론트: "이름 51'" 형식 + **PK/OG 태그** + 추가시간(90+7') 표시.
- 운영 검증: 강원 3:0 광주 → 강투지 10'·모재현 13'·이유현 54'.

---

## 7. 성능 — 라운드 예측 콜드 49s → 0.02s

### 7-1. 스케줄 fetch 메모이즈
- `_fetch_k1/k2_all_games`가 **호출마다 kleague.com 월별 12회 순차 HTTP**(메모이즈 없음) → round-predictions·schedule·예측 등이 매번 반복.
- `_GAMES_CACHE`(30분 TTL) 추가. `invalidate_api_cache`에서 함께 비움.

### 7-2. round-predictions 워밍업 + TTL
- `_warm_cache`가 docstring과 달리 round-predictions를 **안 데우고 있었음** → 부팅 시 현재 라운드(K1·K2) 동적 워밍업 추가.
- TTL 30분 → **6시간**(`_predict_core×경기수`로 계산 비용 큼, 라운드는 주 단위).
- 결과: 현재 라운드 콜드 49.5s → **0.024s**(워밍업 후).

---

## 8. 기타 UX / 데이터
- **새로고침 시 매치 선택 초기화**: Navigation Timing으로 reload/back_forward 판별 → URL 매치 파라미터 제거 + 자동선택 안 함. 공유 링크(navigate) 첫 진입만 자동선택 유지.
- **예상 라인업 배너 제거**: prediction.js의 '이 라인업으로 전술판' CTA + 예상 라인업 카드 + `/api/predicted-lineup` fetch + 헬퍼/캐시 제거. `matchLineupLoaded`(경기에서 불러오기)는 유지.
- **정호연 부상 해제**: `data/player_status.json` → `{}` (DELETE 엔드포인트의 "정상 복귀"와 동일).

---

## 9. 배포 + 검증
- `020bdbf` push → **CI 자동배포**로 가비아 HEAD 자동 반영(서버가 push 직후 020bdbf 도달, git 클린). 서비스 재시작(16:53)으로 새 main.py 로드.
- 운영 검증: https://today-tactics.co.kr HTTP 200, style v54·workspace.js·team_compare v11 라이브, h2h 득점시간 라이브.
- 검증 방식: 헤드리스 Playwright(탭 전환·캔버스 resize·차트 렌더·콘솔 에러 0) + 스크린샷 + 운영 API curl.

---

## 10. 남은 백로그
- **xG 결정력·수비효율 인사이트**: team-compare에 xG-against 없음 → 백엔드 추가 + 기존 xG 계산(선수단위 AVG) 정합성 검증 후. P4/도박맨 Red.
- **경기예측 탭 실브라우저 확인**: 헤드리스에선 스케줄 캐시 콜드 타이밍으로 "분석 중" 멈춤 관측 → 성능 픽스(7장)로 완화됐을 것이나 실브라우저 최종 확인 필요.
- `analytics.js` 미사용 파일 삭제 + CLAUDE.md 파일 목록 갱신.
- 비현재 라운드 첫 조회 ~19s(`_predict_core`) — 필요시 워밍업 범위 확대 or 스탯코어 최적화(Red).
